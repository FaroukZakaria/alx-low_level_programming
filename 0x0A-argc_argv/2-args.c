#include <stdio.h>
/**
 * main - e
 * Return: 0
 * @argc: a
 * @argv: a
 */
int main(int argc, char *argv[])
{
	int i;
		for (i = 0; i < argc; i++)
		{
			printf("%s\n", argv[i]);
		}
		return (0);
}
