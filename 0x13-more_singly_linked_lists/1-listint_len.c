#include "lists.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * listint_len - s
 * @h: s
 * Return: s
 */
size_t listint_len(const listint_t *h)
{
	int i;
		while (h)
		{
			i++;
		}
		return (i);
}
