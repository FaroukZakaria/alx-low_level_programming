#include "main.h"
/**
 * _strchr - s
 * @s: s
 * @c: c
 * Return: s
 */
char *_strchr(char *s, char c)
{
	int i, j, k;
		while (s++)
		{
			j++;
		}
		for (i = 0; i < j; i++)
		{
			if (s[i] == 'c')
			{
				k = i;
				break;
			}
			if ((i = j) && k != i)
			{
				k = 'NULL'
			}
		}
	return (k);
}
