#include "main.h"
/**
 * _islower - omg
 * c - a freaking integer, stop the errors please!
 * Return: 1 for true 0 for false
 */
int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
